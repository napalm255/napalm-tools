# yamllint configuration; actionlint covers what is workflow-specific.
extends: default

rules:
  document-start: disable
  line-length:
    max: 120
  truthy:
    # `on:` is the GitHub Actions trigger key, not a boolean.
    check-keys: false
